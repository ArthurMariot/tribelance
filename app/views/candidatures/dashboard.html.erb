<div class="container">
  <div class="title-applications">
    <h2>My applications </h2>
  </div>
  <div class="candidatures-dashboard">
   <div class="validates-candidatures mt-4">
     <div class="card-mission">
       <div class="card-mission-img">
         <%= cl_image_tag @current_user.avatar, class: "logo-sm" %>
       </div>
       <div class="card-mission-title">
        <h4> <%= @current_user.first_name %> <%= @current_user.last_name %> </h4>
       </div>
       <div class="card-mission-info">
         <p> <i class="fas fa-money-bill-alt"></i> <%= %> $ </p>
         <p> <i class="fas fa-calendar-alt"></i> <%= %> days </p>
         <%=  %>
       </div>
     </div>
     <div class="card-mission mt-2">
       <div class="card-mission-info">
         <p> <%= current_user.candidatures.where(status: "pending").sum(:total_price) %> <i class="fas fa-money-bill-alt"></i>  $ </p>
         <p> <%= current_user.candidatures.where(status: "pending").sum(:num_of_days) %> <i class="fas fa-calendar-alt"></i>  days </p>
       </div>
     </div>
   </div>
   <div class="ok-candidatures">
     <% @candidatures.each do |candidature|  %>
     <div class="ok-candidature">
       <%= cl_image_tag candidature.mission.photo, class: "logo-company-small" %>
       <div class="candidature-content">


              <p> <h6> <%= candidature.mission.company %> </h6> </p>
              <p> Job : <strong><%= candidature.mission.title %></strong> </p>
              <p> <small> Number of days : <%= candidature.num_of_days %> </small> </p>
              <p> <small> Salary : <%= candidature.total_price %> $ </small> </p>
           </div>
           <div class="candidature-actions">
            <% if candidature.status == "accepted" %> <p> Status: <i class="fas fa-check-circle small"></i> (validated) </p>
            <% elsif candidature.status == "pending" %> <p> Status: <i class="fas fa-spinner"></i> (pending) </p>
            <% else %>  <p> Status: <i class="fas fa-times-circle"></i> (declined) </p>
            <% end %>
           </div>
         </div>
       <% end %>

</div>
</div>
